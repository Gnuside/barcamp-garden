= form_for @workshop_attendee do |f|
  - if @workshop_attendee.errors.any?
    #error_explanation
      %h2= "#{pluralize(@workshop_attendee.errors.count, "error")} prohibited this workshop_attendee from being saved:"
      %ul
        - @workshop_attendee.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :comment
    = f.text_area :comment
  .field
    = f.label :user
    = f.text_field :user
  .field
    = f.label :workshop
    = f.text_field :workshop
  .actions
    = f.submit 'Save'
