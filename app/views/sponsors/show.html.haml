%p#notice= notice

%p
	%b Name:
	= @sponsor.name

%p
	= image_tag @sponsor.logo.url
	= image_tag @sponsor.logo.url(:medium)
	= image_tag @sponsor.logo.url(:thumb)

= link_to 'Edit', edit_event_sponsor_path(@event,@sponsor)
\|
= link_to 'Back', event_sponsors_path(@event)
